# The deployed relay, but a non-production build so the loopback match patterns
# stay in the manifest: e2e/live.spec.ts drives the local test video page against
# the real relay, which is the only way to check the live path end to end.
VITE_SERVER_URL=https://abode-relay.bhushangaykawad.workers.dev
VITE_INVITE_BASE_URL=https://abode-relay.bhushangaykawad.workers.dev/j
